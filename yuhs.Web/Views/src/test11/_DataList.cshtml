@using Web.Utilities.Html.Extensions

@model Web.Utilities.Mvc.Foundation.IndexViewModel<yuhs.Entities.src.test11>
        <table class="table table-striped table-bordered table-hover">
            <thead>
            <tr>
		<th class="text-left">Name</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            @if (Model.TargetPager != null && Model.TargetPager.Datas != null)
            {
                foreach (yuhs.Entities.src.test11 item in Model.TargetPager.Datas)
                {
                <tr>
			<td class="text-left">
				@Html.DisplayFor(modelItem => item.Name)
			</td>
                    <td>
                        @Html.ActionLink(" ", "Details", new { id = item.Id, Controller = "test11", area = "yuhs.Web.Views.src"}, new {@class="m_details", title="Details" })
                        @Html.ActionLink(" ", "Edit", new { id = item.Id, Controller = "test11", area = "yuhs.Web.Views.src"}, new {@class="m_edit", title="Edit" })
                        @Html.ActionLink(" ", "#", "#", new {href = "javascript:void(0);",@class="m_delete", title="Delete", onclick = " javascript:metaShare.basic.prototype.deleteTableColumn(this,'" + item.Id + "','" + Url.Action("Delete",new {Controller = "test11", area = "yuhs.Web.Views.src"}) + "'); return false; "})
                    </td>
                </tr>
              }
            }
            </tbody>
        </table>


        <div class="UserPagerDiv text-center">
    @{
        object conditions = new
        {
            Name = Model.SearchModel != null ? Model.SearchModel.Name : "",
        };
    }
        @Ajax.AjaxRenderPager(Model.TargetPager, "Search", "test11","test11_DataList", conditions)
        </div>
